/**
 * @deprecated Import from '@/app/components/dashboard/lib/theme' instead,
 * or better yet use the Tailwind classes (glass-card, dash-btn, dash-input, etc.)
 *
 * This file is a temporary bridge — it re-exports everything from the
 * unified theme so existing admin views keep working without changes.
 */
export {
  glassStyle,
  glassCardStyle,
  statCardStyle,
  glassSectionStyle,
  thStyle,
  tdStyle,
  btnStyle,
  btnPrimaryStyle,
  inputStyle,
  modalOverlayStyle,
  modalStyle,
  tableContainerStyle,
} from '../lib/theme';
